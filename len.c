#include <stdio.h>
void length(char str[]);
int main()
{
	    char str[50];
	        printf("ENTER A STRING\n");
		    scanf("%s",str);
		        length(str);
			   // printf("%d",k);
			        return 0;
			        }
			        void length(char str[])
			        {
			            int i;
			                for(i=0;str[i]!='\0';i++)
			                    {
			                        }    
			                        printf("%d",i);
			              }            
